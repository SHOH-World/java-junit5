package org.co.shoh;

/**
 * The {@code ExecutionData} class represents a runtime data storage
 * for test executions. The ExecutionData is used to {@code save data}
 * so it can be {@code used} or be {@code validated} during end of test run.
 * This will also be {@code independent} of each test run if run in parallel**/
public class ExecutionData {
}
